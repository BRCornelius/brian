import { KidsVideoPipe } from './kids-video.pipe';

describe('KidsVideoPipe', () => {
  it('create an instance', () => {
    const pipe = new KidsVideoPipe();
    expect(pipe).toBeTruthy();
  });
});
